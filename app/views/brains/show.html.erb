<div class="card-brain">

  <div class="card-brain-info">
    <h2><%= @brain.name %></h2>
    <h5><em><%= @brain.category%></em></h5>
    <p><%= @brain.description %></p>
    <p><%= @brain.price %> SimFlouz/heure</p>
      <%= image_tag "https://source.unsplash.com/random/700x100/?fruit", class:"brain-img" %>
  </div>

  <% if current_user.id == @brain.user_id %>
    <%= link_to "Me mettre à jour! 🧠", edit_brain_path(@brain), class:"text-decoration-none text-white btn-gradient" %>
    <%= link_to "Hasta la Vista, Baby! 🧠", brain_path(@brain), data: { turbo_method: :delete, turbo_confirm: "Are you sure ?"}, class:"text-decoration-none text-white btn-gradient" %>
  <% end %>

  <div data-controller="toggle" class="mt-5">
    <button class="btn-gradient text-decoration-none text-black" data-action="click->toggle#book">Réserve moi! 🧠</button>
    <div data-toggle-target="toggleableElement" class="d-none">
      <%= render "/shared/formbooking", booking: @booking %>
    </div>
  </div>
</div>
